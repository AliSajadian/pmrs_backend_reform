"""
ProjectsFiles config for the projects_files application.
"""
from django.apps import AppConfig


class ProjectsFilesConfig(AppConfig):
    """
    ProjectsFiles config for the projects_files application.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'projects_files'
