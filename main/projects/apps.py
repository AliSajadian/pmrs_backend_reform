"""
Projects config for the projects application.
"""
from django.apps import AppConfig


class ProjectInfoConfig(AppConfig):
    """
    Projects config for the projects application.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'projects'
